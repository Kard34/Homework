#include <stdio.h>

int main()
{
    int Even = 0;
    for (int i = 0; i < 4; i++)
    {
        int num;
        scanf("%d", &num);
        if (num % 2 == 0)
            Even++;
    }
    printf("%d", Even);
    return 0;
}
