#include <stdio.h>
#include <limits.h>

int main()
{
    int max = INT_MIN;
    for (int i = 0; i < 3; i++)
    {
        int num;
        scanf("%d", &num);
        if (num > max)
            max = num;
    }
    printf("%d", max);
    return 0;
}
