#include <stdio.h>
#include <string.h>

int main()
{
    char text[10000];
    scanf("%[^\n]", text);
    int n = strlen(text);
    for (int i = n - 1; i >= 0; i--)
    {
        printf("%c", text[i]);
    }
    return 0;
}
